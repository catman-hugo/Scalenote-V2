{
  "$schema": "../../node_modules/@tauri-apps/cli/schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for ScaleNote",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-close",
    "dialog:allow-open",
    "dialog:allow-save",
    "shell:allow-open"
  ]
}
